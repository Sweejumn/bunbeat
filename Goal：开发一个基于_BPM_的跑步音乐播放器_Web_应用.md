# Goal：开发一个基于 BPM 的跑步音乐播放器 Web 应用

## 1. 项目目标

开发一个简单、可实际使用的 Web 音乐播放器，主要面向需要进行校园跑、日常跑步或快走的学生。

核心功能是：

> 用户提供自己的音乐文件或合法可处理的音乐来源 → 系统分析歌曲 BPM → 根据用户选择的运动强度推荐合适歌曲 → 将歌曲调整到统一目标 BPM → 可选地加入节拍器 → 在网页播放器中连续播放。

第一阶段以 **MVP 可运行、功能闭环、代码结构清晰** 为最高优先级，不要为了扩展功能而牺牲基本功能的稳定性。

---

# 2. 核心用户流程

用户进入网站后：

1. 上传若干首自己的音乐文件。
2. 系统自动分析每首歌曲的：
   - 歌曲名称
   - 时长
   - BPM
   - BPM 分析可信度（如果工具能够提供）
3. 用户选择运动类型：
   - 走路
   - 慢跑
   - 跑步
   - 快跑
   - 自定义 BPM
4. 系统根据目标 BPM 对歌曲进行排序/筛选。
5. 用户查看推荐结果。
6. 用户可以选择歌曲。
7. 系统将选中的歌曲处理为目标 BPM。
8. 用户可以选择是否加入节拍器。
9. 在网页播放器中播放处理后的音乐。
10. 播放器应显示：
    - 当前歌曲
    - 当前 BPM / 目标 BPM
    - 播放进度
    - 播放/暂停
    - 上一首/下一首
    - 音量
    - 节拍器状态

---

# 3. MVP 功能范围

## 3.1 音乐导入

第一版只要求支持：

- MP3
- WAV
- M4A（如果当前技术方案方便支持）

支持一次上传多个文件。

**暂时不要实现从网易云音乐、QQ 音乐、Spotify 等平台抓取或下载受版权保护的音频。**

如果未来需要支持歌单链接，第一阶段可以只设计接口和数据模型，不实现受限制的音频抓取。

---

## 3.2 BPM 分析

后端对上传的歌曲进行 BPM 分析。

要求：

- 使用成熟的 BPM/beat detection 工具，不要自己从零实现完整 BPM 检测算法。
- 分析过程应该异步执行，避免阻塞 Web 请求。
- 前端显示分析状态，例如：

```text
正在分析 3 / 10
```

完成后显示：

```text
Song A     128 BPM
Song B     143 BPM
Song C     167 BPM
```

需要考虑 BPM 检测存在 2 倍/0.5 倍错误的问题。

例如检测结果为 64 BPM，但音乐实际更可能是 128 BPM 时，应尽可能进行合理修正，或者允许用户手动修改 BPM。

---

# 4. 运动模式

预设以下模式：

```text
走路       100–120 BPM
慢跑       120–145 BPM
跑步       145–165 BPM
快跑       165–185 BPM
自定义     用户指定 BPM
```

这些范围只是默认值，不要把它们设计成不可修改的医学或运动标准。

用户选择运动类型后：

```text
目标 BPM
↓
寻找最接近目标 BPM 的歌曲
↓
按照原 BPM 与目标 BPM 的差距进行排序
```

例如目标为 165 BPM：

```text
163 BPM   ★★★★★
167 BPM   ★★★★★
158 BPM   ★★★★
150 BPM   ★★★
128 BPM   ★
```

---

# 5. BPM 统一处理

这是本项目最重要的音频功能之一。

用户选择目标 BPM 后：

```text
原 BPM → 目标 BPM
128    → 165
143    → 165
156    → 165
172    → 165
```

需要使用 **time stretching / tempo adjustment** 技术改变歌曲速度。

### 重要要求

尽可能做到：

> 改变歌曲速度，但保持原歌曲音高基本不变。

不要简单使用：

```text
playbackRate = targetBpm / originalBpm
```

因为这种方式会同时改变音高。

优先使用成熟的音频处理工具，例如：

- FFmpeg
- Rubber Band
- SoundTouch
- 其他成熟的 time-stretch 实现

具体技术方案由 Agent 根据项目环境选择。

---

# 6. 音频处理策略

不要默认把所有歌曲永久生成大量新音频文件。

优先考虑：

```text
原始歌曲
    ↓
分析 BPM
    ↓
用户确定目标 BPM
    ↓
生成临时处理文件
    ↓
播放
```

如果实际技术方案更适合缓存处理结果，可以缓存：

```text
song_id + target_bpm + metronome_settings
```

对应的处理结果。

需要避免重复处理同一首歌曲。

---

# 7. 节拍器

播放器提供：

```text
☐ 节拍器
```

开启后，在音乐中加入与目标 BPM 对齐的节拍。

至少支持：

- 开启/关闭
- 音量调整

最好支持：

```text
普通节拍
强节拍
```

例如每 4 拍一个重拍：

```text
● · · · | ● · · · | ● · · ·
```

节拍器不能明显破坏原音乐。

如果后端生成音频较复杂，可以优先实现浏览器端节拍器。

---

# 8. 播放器

实现一个简洁的现代 Web Audio Player。

至少包含：

```text
歌曲名称
歌手/文件名
当前 BPM
目标 BPM

播放进度
━━━━━━━━━━━━━━

⏮  ▶/⏸  ⏭

🔊 音量

节拍器 ON/OFF
```

支持播放列表连续播放。

用户可以：

- 播放
- 暂停
- 上一首
- 下一首
- 拖动进度
- 调节音量
- 切换节拍器
- 查看当前歌曲 BPM

---

# 9. UI / UX

整体设计应该简单，不要做成复杂音乐社区。

目标用户主要是：

> 大学生 / 校园跑用户 / 普通跑步用户。

界面应该让用户在几秒内理解：

```text
我有什么歌
↓
我今天想怎么跑
↓
系统推荐什么
↓
开始跑
```

首页可以包含：

```text
RUN BPM

让音乐跟随你的跑步节奏

[上传音乐]

运动模式：

[🚶 走路]
[🏃 慢跑]
[🏃 跑步]
[⚡ 快跑]
[🎯 自定义]

目标 BPM：165

[生成推荐]

----------------------------

推荐歌曲

歌曲 A       163 BPM
歌曲 B       167 BPM
歌曲 C       158 BPM

[开始播放]
```

移动端必须基本可用，因为用户跑步时很可能使用手机。

---

# 10. 推荐算法

第一版不要设计复杂 AI 推荐算法。

使用简单、可解释的规则：

```text
distance = abs(song_bpm - target_bpm)
```

按照 distance 从小到大排序。

同时考虑：

- BPM 检测可信度
- 音频是否能够成功处理
- 用户是否已经播放过
- 用户是否手动排除歌曲

以后再扩展机器学习或用户偏好。

---

# 11. 技术架构

推荐：

### Frontend

```text
React
TypeScript
```

可以使用：

```text
Vite
Tailwind CSS
```

### Backend

优先考虑：

```text
Python
FastAPI
```

因为项目包含 BPM 分析和音频处理。

### Database

MVP：

```text
SQLite
```

即可。

如果项目规模扩大，再考虑 PostgreSQL。

### Audio

根据实际测试选择成熟工具：

```text
FFmpeg
Rubber Band
SoundTouch
librosa
Essentia
```

不要为了“自己实现算法”而重新实现成熟的音频处理能力。

---

# 12. 数据模型

至少设计以下概念：

### Song

```text
id
filename
title
artist
duration
original_bpm
bpm_confidence
created_at
```

### ProcessingTask

```text
id
song_id
target_bpm
metronome_enabled
status
output_path
created_at
```

如果实际实现不需要数据库记录 ProcessingTask，可以采用更简单的缓存方案。

---

# 13. API

根据实际架构设计 REST API。

至少需要类似：

```text
POST /api/songs/upload
GET  /api/songs
GET  /api/songs/{id}
POST /api/songs/{id}/analyze

POST /api/recommend
POST /api/process

GET  /api/audio/{id}

GET  /api/health
```

具体 API 可以根据实际实现调整。

---

# 14. 工程要求

代码必须：

- 模块化
- 有清晰的 frontend/backend 分离
- 使用 Git
- 提供 README
- 提供启动方式
- 提供环境变量示例
- 不把密钥提交到 Git
- 不把用户上传的音频提交到 Git
- 不硬编码本机绝对路径
- 不依赖开发者电脑上的特殊环境

README 至少说明：

```text
项目简介
功能
技术栈
环境要求
安装
启动
使用方法
音频处理依赖
常见问题
项目结构
```

---

# 15. 错误处理

必须考虑：

### BPM 分析失败

显示：

```text
无法可靠检测 BPM
```

并允许用户手动输入 BPM。

### 音频格式不支持

明确提示用户。

### Time Stretch 失败

不能让整个播放器崩溃。

### 文件过大

限制上传大小，并给出明确提示。

### 多个任务同时处理

需要避免服务器资源被无限占用。

---

# 16. 隐私与版权边界

第一版：

> 用户上传自己的音乐文件，仅用于本地/服务器端处理和播放。

不要实现：

- 破解音乐平台
- 绕过 DRM
- 从 Spotify/网易云/QQ 音乐等平台下载受保护音频
- 抓取受限制的音频流
- 将处理后的商业音乐公开提供给其他用户下载

如果未来加入第三方音乐平台：

> 优先使用官方 API 获取歌单元数据和官方允许的播放能力。

不要通过绕过平台限制的方式实现。

---

# 17. 开发顺序

按照以下顺序实现，不要一开始就做所有功能：

### Phase 1：基础播放器

```text
上传 MP3
↓
网页播放
↓
播放列表
```

### Phase 2：BPM 分析

```text
上传
↓
BPM Detection
↓
显示 BPM
```

### Phase 3：推荐

```text
选择运动模式
↓
计算目标 BPM
↓
推荐歌曲
```

### Phase 4：BPM 调整

```text
原歌曲
↓
Time Stretch
↓
目标 BPM
```

### Phase 5：节拍器

```text
目标 BPM
↓
Metronome
↓
音乐同步播放
```

### Phase 6：UI/UX

最后统一优化：

- 手机端
- 加载状态
- 错误提示
- 播放体验
- 动画
- 页面布局

---

# 18. 验收标准

项目完成后必须能够实际完成以下完整流程：

```text
启动项目
    ↓
打开网页
    ↓
上传至少 5 首 MP3
    ↓
系统成功分析 BPM
    ↓
选择「跑步」
    ↓
得到推荐歌曲
    ↓
选择目标 BPM，例如 165
    ↓
成功处理歌曲
    ↓
歌曲播放
    ↓
音高基本保持不变
    ↓
歌曲节奏变为接近目标 BPM
    ↓
开启节拍器
    ↓
节拍器与目标 BPM 同步
    ↓
下一首歌曲继续播放
```

至少测试：

```text
120 → 165 BPM
150 → 165 BPM
165 → 165 BPM
180 → 165 BPM
```

确认音频不会出现明显爆音、严重失真、播放中断或播放器卡死。

---

# 19. 开发原则

最重要的原则：

1. **先做出能运行的 MVP。**
2. 不要过度设计。
3. 不要为了未来功能提前建立复杂架构。
4. 优先使用成熟的音频处理库。
5. 遇到平台限制时，不要通过绕过限制的方式解决。
6. 每完成一个主要阶段都进行实际测试。
7. 如果某个技术方案不可行，及时调整方案，而不是在错误方向继续堆代码。
8. 所有关键功能必须能够实际运行，而不是只创建接口或模拟数据。
9. 最终项目应该是一个可以在本地直接启动并演示的完整 Web 应用。

## 最终目标

最终得到一个真正可运行的 MVP：

> **上传自己的音乐 → 自动识别 BPM → 选择走路/跑步/快跑 → 推荐合适歌曲 → 将歌曲统一到目标 BPM → 可选节拍器 → 连续播放。**

优先保证这个闭环完整，再考虑歌单链接、多平台接入、账号系统、云端存储、用户偏好、跑步数据等扩展功能。